{
  "name": "nuxt-ui-code-lens",
  "displayName": "Nuxt UI Code Lens",
  "description": "Nuxt UI documentation and helpers inside VS Code",
  "version": "0.0.1",
  "publisher": "leVund",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "nuxt",
    "nuxt-ui",
    "vue",
    "documentation"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onLanguage:vue"
  ],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "nuxtUiCodeLens",
          "title": "Nuxt UI Code Lens",
          "icon": "resources/nuxt-ui-icon.svg"
        }
      ]
    },
    "views": {
      "nuxtUiCodeLens": [
        {
          "icon": "resources/nuxt-ui-icon.svg",
          "type": "webview",
          "id": "nuxtUiCodeLens.docView",
          "name": "Documentation"
        }
      ]
    },
    "configuration": {
      "title": "Nuxt UI Code Lens",
      "properties": {
        "nuxtUiCodeLens.version": {
          "type": "string",
          "enum": [
            "auto",
            "v3",
            "v4"
          ],
          "default": "auto",
          "description": "Which Nuxt UI documentation version to display. 'auto' reads the installed @nuxt/ui version from the workspace package.json."
        },
        "nuxtUiCodeLens.codeLens.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Show a CodeLens above lines containing Nuxt UI component tags."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "bun run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.4.0"
  }
}
