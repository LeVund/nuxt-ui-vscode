{
  "name": "nuxt-ui-vscode",
  "displayName": "Nuxt UI helper",
  "description": "Nuxt UI documentation and helpers inside VS Code",
  "version": "0.0.1",
  "publisher": "leVund",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "nuxt",
    "nuxt-ui",
    "vue",
    "documentation"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onLanguage:vue"
  ],
  "contributes": {
    "commands": [
      {
        "command": "nuxtUiHelper.openHome",
        "title": "Nuxt UI helper: Open Documentation Home"
      },
      {
        "command": "nuxtUiHelper.openComponent",
        "title": "Nuxt UI helper: Open Component Docs..."
      },
      {
        "command": "nuxtUiHelper.showComponentMenu",
        "title": "Nuxt UI helper: Show Component Actions"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "nuxtUiHelper.showComponentMenu",
          "when": "false"
        }
      ]
    },
    "configuration": {
      "title": "Nuxt UI helper",
      "properties": {
        "nuxtUi.version": {
          "type": "string",
          "enum": [
            "auto",
            "v3",
            "v4"
          ],
          "default": "auto",
          "description": "Which Nuxt UI documentation version to display. 'auto' reads the installed @nuxt/ui version from the workspace package.json."
        },
        "nuxtUi.inlayHints.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Show a clickable inlay hint next to Nuxt UI component tags."
        },
        "nuxtUi.hover.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Show a hover tooltip with actions on Nuxt UI component tags."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "bun run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.4.0"
  }
}
